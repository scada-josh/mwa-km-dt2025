export interface statCardProps {
  title: string
  value: string
  interval?: string
  trend?: string
  data:number[]
}

export interface categoryProps {
  // data:number[]
  category: string
  totalSales: string
}

